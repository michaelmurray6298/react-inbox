import reducer from './index.js';
import * as types from '../actions/index.js';

describe('messages reducer', () => {
  it('should return the inital state with messages', () => {
    const intialState = {
      messages: [],
    };
  });
});
